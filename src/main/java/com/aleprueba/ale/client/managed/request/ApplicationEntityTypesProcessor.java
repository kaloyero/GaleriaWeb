// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.

package com.aleprueba.ale.client.managed.request;

import com.google.gwt.requestfactory.shared.EntityProxy;
import java.util.Collections;
import java.util.HashSet;
import java.util.Set;

public abstract class ApplicationEntityTypesProcessor<T> {

    private final T defaultValue;

    private T result;

    public ApplicationEntityTypesProcessor() {
        defaultValue = null;
    }

    public ApplicationEntityTypesProcessor(T defaultValue) {
        this.defaultValue = defaultValue;
    }

    public static Set<java.lang.Class<? extends com.google.gwt.requestfactory.shared.EntityProxy>> getAll() {
        Set<Class<? extends EntityProxy>> rtn = new HashSet<Class<? extends EntityProxy>>();
        rtn.add(ModelProxy.class);
        rtn.add(CuentasProxy.class);
        return Collections.unmodifiableSet(rtn);
    }

    private static void process(com.aleprueba.ale.client.managed.request.ApplicationEntityTypesProcessor<?> processor, Class<?> clazz) {
        if (ModelProxy.class.equals(clazz)) {
            processor.handleModel((ModelProxy) null);
            return;
        }
        if (CuentasProxy.class.equals(clazz)) {
            processor.handleCuentas((CuentasProxy) null);
            return;
        }
        processor.handleNonProxy(null);
    }

    private static void process(com.aleprueba.ale.client.managed.request.ApplicationEntityTypesProcessor<?> processor, Object proxy) {
        if (proxy instanceof ModelProxy) {
            processor.handleModel((ModelProxy) proxy);
            return;
        }
        if (proxy instanceof CuentasProxy) {
            processor.handleCuentas((CuentasProxy) proxy);
            return;
        }
        processor.handleNonProxy(proxy);
    }

    public void handleNonProxy(Object object) {
    }

    public abstract void handleModel(ModelProxy proxy);

    public abstract void handleCuentas(CuentasProxy proxy);

    public T process(Class<?> clazz) {
        setResult(defaultValue);
        ApplicationEntityTypesProcessor.process(this, clazz);
        return result;
    }

    public T process(Object proxy) {
        setResult(defaultValue);
        ApplicationEntityTypesProcessor.process(this, proxy);
        return result;
    }

    protected void setResult(T result) {
        this.result = result;
    }
}
